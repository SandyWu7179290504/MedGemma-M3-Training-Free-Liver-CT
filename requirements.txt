# Organ-Aware MedGemma 1.5 — 肝臟病灶偵測 Pipeline
# Python >= 3.10

# PyTorch: 請依 CUDA 版本另行安裝，例如：
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cu118

# 模型與推論
transformers>=4.52
accelerate
bitsandbytes
huggingface-hub

# 醫學影像
nibabel
scikit-image
scipy

# 視覺化與通用
numpy
matplotlib
Pillow

# Organ-Aware Pipeline（使用 TotalSegmentator 時需要）
TotalSegmentator
